# InsightX — Python Package Requirements
# Install with:  pip install -r requirements.txt
#
# NOTE: tkinter is a Python built-in and is NOT listed here.
#   Windows : reinstall Python from python.org and tick the tcl/tk option
#   Ubuntu  : sudo apt install python3-tk
#   macOS   : brew install python-tk  OR use the python.org installer

# ── Core data & visualisation ─────────────────────────────────────────────────
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0

# ── AI / LLM stack ───────────────────────────────────────────────────────────
# PyTorch — install the CUDA build for GPU acceleration (see note below)
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# ── 4-bit quantisation (GPU only; safe to skip on CPU-only machines) ──────────
bitsandbytes>=0.41.0

# ── Optional — significantly speeds up inference on CUDA GPUs ─────────────────
# flash-attn>=2.0.0   # uncomment if you have a modern NVIDIA GPU + CUDA 11.8+

# ─────────────────────────────────────────────────────────────────────────────
# GPU / CUDA NOTE
# ─────────────────────────────────────────────────────────────────────────────
# The default `pip install torch` installs the CPU build.
# For CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Verify your CUDA version with:  nvcc --version

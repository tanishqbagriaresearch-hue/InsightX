# InsightX — Python dependencies
# Install with: pip install -r requirements.txt
#
# Note: llama-cpp-python requires a C++ compiler.
#   GPU (CUDA):  CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python
#   GPU (Metal): CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python
#   CPU only:    pip install llama-cpp-python

pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
llama-cpp-python>=0.2.0
